# Copy to ~/.config/t560-music-panel/config.ini and replace every entity ID
# with the actual IDs created by Home Assistant and the Media Controller
# integration. There are no text-entry controls in the tablet application.

[home_assistant]
url=http://192.168.1.100:8123

[entities]
player=media_player.your_music_assistant_player
queue=sensor.your_controller_queue
playlists=sensor.your_controller_playlists

# These room controls are optional. Remove a key to disable its tile.
light_1=light.your_controller_light_1
light_2=light.your_controller_light_2
fan=switch.your_controller_fan
ac=switch.your_controller_ac

[labels]
light_1=LIGHT 1
light_2=LIGHT 2
fan=FAN
ac=AC

[panel]
# 1000 ms is responsive while keeping CPU/network use low.
poll_interval_ms=1000
# Playlist state can be much larger and changes infrequently.
playlist_poll_interval_ms=60000
# Turn the display off after this many seconds without touchscreen or key
# input. Use 0 to keep the display on until the Power button is pressed.
# Values are clamped to 5-3600 seconds. The Power and Home button handler
# reads this key at start-up.
screen_off_seconds=30
